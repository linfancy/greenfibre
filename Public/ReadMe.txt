这模板主要是为了做自己的小项目的时候能够免去麻烦的准备工作。

模板为了兼容低版本IE，引入不少shim，不需要的话可以删除：
  html5shiv.js--让<=IE8支持html5标签
  DD_belatedPNG.js--让IE6支持png透明背景
  PIE--让低版本IE支持常用的css3属性


同样是为了兼容低版本IE，使用的jQuery、normalize都是低版本的，根据情况可以考虑使用最新版本。
